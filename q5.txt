1. No the change inside increment_int_a does not change the variable a after the function call, the variable a value won't be incremented 
2. Yes the change inside increment_int_b will change the variable b after the function call because the variable b value will be incremented.
3. The reason for the difference in answers is the way of initializing data types and scope. 
